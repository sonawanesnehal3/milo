{
  "name": "milo-global-footer",
  "version": "0.0.1",
  "description": "Milo footer",
  "scripts": {
    "build": "node build.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  
  "devDependencies": {
    "esbuild": "^0.19.8"
  }
}
